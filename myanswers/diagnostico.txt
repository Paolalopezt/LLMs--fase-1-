DIAGNÓSTICO - answer-0514.py (segmentar_rutas)

El validador clasificó esta solución como "usecase generator error", 
no como "answers with run errors" ni "answers incorrect", lo que indica 
que el error está en el generador de casos de uso del compañero, 
no en mi solución.

CAUSA DEL ERROR:
El archivo del generador (question-0002-usecase-generator.py de santiagoparra0603)
define su propia función llamada segmentar_rutas en las líneas 8-35 del generador.
Cuando el validador ejecuta el generador con exec(), esa definición interna 
pisa/sobreescribe mi función segmentar_rutas del archivo answer-0514.py.

Esto provoca que al momento de llamar segmentar_rutas(*input_args), 
el validador ejecuta la versión del generador (que espera ser llamada 
internamente con argumentos ya preparados) en lugar de mi solución,
resultando en el error: "segmentar_rutas() missing 1 required positional argument: 'X'"

EVIDENCIA:
- Mi solución define correctamente: def segmentar_rutas(X, random_state=42)
- El generador también define: def segmentar_rutas(X: np.ndarray, random_state: int = 42)
  en sus líneas 8-35, creando el conflicto de nombres.
- El validador reportó 0 "answers with run errors" y 0 "answers incorrect",
  confirmando que mi código no tiene errores propios.
